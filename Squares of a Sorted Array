class Solution {
public:
    vector<int> sortedSquares(vector<int>& nums) 
    {
        int size=nums.size();
        vector<int> ans(size);
        for(int i=0;i<size;i++)
        {
            ans[i]=nums[i]*nums[i];
        }
        sort(ans.begin(),ans.end());
        return ans;
    }
};
